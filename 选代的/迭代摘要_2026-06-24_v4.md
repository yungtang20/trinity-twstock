---
created: 2026-06-24T10:00:00+08:00
task: 全面 Supabase 數據修復與優化
status: completed
---

# Supabase 數據修復與優化 - 迭代摘要

## 本次任務
全面檢查並修復 Supabase 數據抓取、同步、寫入的完整流程。

## 執行過程

### 1. 修復 Supabase RLS 政策 ✅
- 檔案：`scripts/create_supabase_tables.sql`
- 為所有 6 張表新增 INSERT/UPDATE 政策
- 保留原有的 SELECT 匿名讀取政策

### 2. 統一 Supabase 表結構定義 ✅
- 修正 TDCC 寫入表名：`stock_features` → `shareholding_unified`
- 修正 dashboard 查詢表名：
  - `recent-dividend`：`stock_features` → `dividend_events`
  - `trust-buy-2day`：`stock_institutional` → `institutional_data`
  - `break-ma200` / `limit-up-yesterday`：`stock_features` → `shareholding_unified`
- 修正 MarketsView 查詢表名：`tdcc_shareholding` → `shareholding_unified`
- 為 `buildSupabaseRows` 新增 `source` 欄位

### 3. 將主要資料寫入 Supabase ✅
- 新建 `src/server/services/supabase-sync.ts`
  - `syncStockHistory()`：同步股價歷史
  - `syncInstitutionalData()`：同步法人籌碼
  - `syncDividendEvents()`：同步除權息
- 修改 `finmind.ts`：回填完成後自動同步至 Supabase
- 使用 `onConflict` 參數避免重複資料

### 4. 增加 Supabase 寫入重試與事務保護 ✅
- 建立 `upsertWithRetry()` 通用重試函數
- 指數退避策略：1s → 2s → 4s
- 最大重試次數：3 次
- TDCC 同步改用 `shareholding_unified` 表

### 5. 統一 API 回應格式與錯誤處理 ✅
- 新建 `src/server/lib/api-res.ts`
  - `json()`：成功回應
  - `error()`：錯誤回應
  - `wrap()`：自動錯誤包裝
- 新建 `src/server/services/capacity-monitor.ts`
  - 資料庫容量監控
  - 警告等級：ok/caution/warning/critical
- 修改 `settings.ts`：新增 `/api/supabase/usage` 端點

## 迭代狀態
- 所有 5 項任務已完成
- 伺服器狀態：正常
- 下次檢查：待命

## 期望輸出
1. Supabase 表格結構與程式碼完全一致
2. 所有資料來源（FinMind、TDCC、TWSE、TPEX）都能同步寫入 Supabase
3. 寫入失敗時自動重試（最多 3 次）
4. 前端能正確查詢 Supabase 資料
5. API 回應格式統一
6. 資料庫容量監控功能正常
