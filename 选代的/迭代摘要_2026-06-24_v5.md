---
created: 2026-06-24T10:30:00+08:00
task: 修正首頁指數顯示重複、錯誤和資料來源問題
status: completed
---

# 首頁指數顯示修復 - 迭代摘要

## 本次任務
修正首頁（DashboardView）中指數顯示重複、錯誤和資料來源不一致的問題。

## 執行過程

### 發現的問題

| # | 嚴重度 | 問題 | 位置 |
|---|--------|------|------|
| 1 | 高 | OTC 櫃買指數漲跌百分比計算錯誤 | `otc.ts:35` |
| 2 | 中 | TWSE 指數重複顯示兩次 | `DashboardView.tsx:459,487` |
| 3 | 中 | OTC 指數數值未顯示 | `DashboardView.tsx:463` |
| 4 | 低 | DataSourceSelector 硬編碼為 "local" | `DashboardView.tsx:143` |

### 修正項目

1. **修正 OTC 漲跌百分比計算**
   - 檔案：`src/server/services/otc.ts`
   - 修改：從自行計算改為直接讀取 API 回傳的 `row[6]`
   - 原因：自行計算產生浮點數誤差

2. **修正 DashboardView 重複顯示**
   - 檔案：`src/components/views/DashboardView.tsx`
   - 新增 `otcStats` state 變數
   - 新增 OTC stats 的 `useEffect` 載入邏輯
   - 修正 Header bar 顯示：改為同時顯示加權和櫃買指數
   - 修正 OTC 標題：顯示指數數值

3. **修正 DataSourceSelector**
   - 在 `CollapsibleCard` 组件中加入 `useState<DataSource>("local")`
   - 將硬編碼的 `"local"` 改為動態的 `source` 變數

## 測試結果
- TypeScript 編譯：✅ 無新增錯誤
- 伺服器狀態：✅ 正常運作
- Port 3000：✅ 監聽中

## 迭代狀態
- 所有修正已完成
- 下次檢查：待命

## 期望輸出
1. 首頁只顯示一次加權指數和櫃買指數
2. OTC 指數數值正確顯示
3. 漲跌百分比使用 API 回傳值，非自行計算
4. DataSourceSelector 能正確反映當前資料來源
