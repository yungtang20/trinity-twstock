# Optional dependencies needed only when enabling the vendored Kronos engine.
# Install after requirements.txt and select a torch build compatible with your
# Python version and hardware from the official PyTorch index when necessary.
einops>=0.8,<1
huggingface-hub>=0.27,<1
safetensors>=0.4.3,<1
torch>=2.5,<3
tqdm>=4.67,<5
