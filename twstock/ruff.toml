line-length = 120
extend-exclude = ["vendor", "backups", ".pytest_codex_*"]

[lint]
select = ["E", "F"]
# Existing console entry points intentionally bootstrap package imports before
# later imports; long bilingual UI strings are also not useful lint failures.
ignore = ["E402", "E501"]
