[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    -ra
markers =
    e2e: End-to-end tests that call real APIs
    slow: Tests that might take longer to run
    integration: Integration tests
