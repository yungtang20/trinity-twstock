# GATES.md - 验收闸门

状态：`未配置`

本文件定义“什么算完成”。`check.sh` 是可执行版本。

## 1. 总规则

只有同时满足以下条件，才可以声称完成：

1. `bash check.sh` 通过。
2. 满足 `PRD.md` 的相关验收标准。
3. 如果有前端，符合 `FRONTEND_HANDOFF.md` 的交互和 UI 要求。
4. 没有引入未经确认的新依赖、架构变化或密钥硬编码。

## 2. 自动检查

技术栈确认后，把真实命令填进这里和 `check.sh`。

| 闸门 | 命令 | 是否必须 | 通过条件 |
|---|---|---|---|
| 环境 / 安装 | | 是 | 退出码 0 |
| 构建 / 编译 | | 是 | 退出码 0，无报错 |
| 测试 | | 是 | 全部通过 |
| 类型检查 | | 视情况 | 无类型错误 |
| lint / 格式 | | 视情况 | 无 error |
| 冒烟测试 | | 是 | 主流程可启动 |
| 安全 / 依赖审计 | | 视情况 | 无高危 |

## 3. 人工验收

- [ ] P0 主流程可端到端完成。
- [ ] 空、加载、错误、成功状态都可见且合理。
- [ ] 错误场景有反馈，不会让用户卡死。
- [ ] 当前版本已写入 `STATUS.md`。
- [ ] 历史版本已写入 `CHANGELOG.md`。
- [ ] 新报错或踩坑已写入 `ERRORS.md`。

## 4. 最近运行

- 运行时间：
- 运行命令：`bash check.sh`
- 结果：
- 未通过项：
