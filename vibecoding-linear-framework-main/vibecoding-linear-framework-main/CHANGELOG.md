# CHANGELOG.md - 历史版本

默认只在查历史、记录版本、验收发布时读取。
最新版本放在最上面。

| 版本号 | 日期 | 类型 | 改动摘要 | 对应步骤 / 里程碑 | 备注 |
|---|---|---|---|---|---|
| | | feat/fix/docs/refactor/chore | | | |

## 版本规则

- `0.x.x`：开发期版本。
- `x.y.z`：语义化版本；功能加 `y`，修复加 `z`，破坏性变化加 `x`。
- 每完成一个可运行版本，都在表格顶部加一行。

## 里程碑备注

- 
